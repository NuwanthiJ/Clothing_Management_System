import { useState } from 'react'
import 'bootstrap/dist/css/bootstrap.min.css'
import Login from './Login'
import {BrowserRouter, Routes,Rout, BrowserRouter} from 'react-router-dom'

function App(){
    const {count,setCount} =UseState(0)

    return(
        <BrowserRouter>
         <Routes>
             

         </Routes>
        </BrowserRouter>
       
    )
}

export default App